spring:
  datasource:
    url: jdbc:h2:mem:courses_api;MODE=MySQL
    username: sa
    password: password
    driverClassName: org.h2.Driver
#  h2:
#    console:
#      enabled: true
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    properties:
      hibernate:
#        generate_statistics: true
        enable_lazy_load_no_trans: true
    hibernate:
      ddl-auto: update